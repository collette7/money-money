# Supabase - Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-only - Never expose these in client code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Password - Only for migration scripts (optional)
# SUPABASE_DB_PASSWORD=your-database-password

# Encryption Key (REQUIRED) - Generate with: openssl rand -base64 32
# After setting, run: node scripts/encrypt-existing-tokens.js
ENCRYPTION_KEY=your-32-byte-base64-key

# Optional: Rate Limiting (Upstash Redis)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Optional: Monitoring
# SENTRY_DSN=

# Optional: Feature Flags
# ENABLE_INVESTMENTS=false
# ENABLE_PARTNERS=false
